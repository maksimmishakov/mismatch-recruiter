meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11
build:
  requirementsPath: requirements.txt
run:
  scriptName: app.py
  command: gunicorn app:app --bind 0.0.0.0:80
  containerPort: 80

meta:
  environment: python
  version: 3.11

build:
  requirementsPath: requirements.txt

run:
  scriptName: wsgi.py
  command: gunicorn wsgi:app --bind=0.0.0.0:5000 --workers 4
  containerPort: 5000

volumes:
  - mount: /data
    size: 1Gb

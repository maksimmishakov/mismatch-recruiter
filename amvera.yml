---
app:
  id: mismatch-recruiter
  branch: main
  runtime:
    language: python
    version: '3.11'
  build:
    installDependencies: true
  run:
    cmd: gunicorn -w 4 -b 0.0.0.0:8080 wsgi:app
  env:
    DATABASE_URL: sqlite:///mismatch.db
    FLASK_ENV: production
    SECRET_KEY: ${SECRET_KEY}
